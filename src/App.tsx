import * as React from 'react';
import {
  Container,
} from '@mui/material';
import MiniDrawer from './shop-page';

const App: React.FC = () => (
  <Container>
    <MiniDrawer />
  </Container>
  );

export default App;
